# Gitleaks configuration for Hive
# https://github.com/gitleaks/gitleaks#configuration

[allowlist]
  description = "Known false positives"

  # curl -u "$SONAR_TOKEN:" in ci.yml is a GitHub Actions secret reference,
  # not a hardcoded credential.
  [[allowlist.commits]]
    description = "SonarCloud SARIF fetch uses Actions secret via curl -u"
    commit = "2f2c52c2fdae0d0f596188e955538d0f597b6a46"
